
# placeholder for making set up repeatable
# Set up ssh server
#  install
#  start daemon
#  set up ssh key (ssh-copy-id from ssh client)
#  turn off password verification
#  use ubuntu firewall to block anything but local access to port 22
#  set up static ip lease on router

# Torrents
# install transmission-cli

# had to set intel_idle.max_cstate=1 in grub linux default (c state bug in processor)
